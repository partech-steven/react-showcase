import React, { Component } from "react";

import './timer.css';

export class Timer extends Component {
    render() {
        return (
            <div className="task">
                I am a timer
            </div>
        );
    }
};